#include <stdio.h>

/**
 * main -> print single digit numbers of base 10
 * folowed by a new line
 * return/: always 0
 */

int main(void)
{
	int ch;

	for (ch = 49; ch <= 57; ch++)
	{
		putchar(ch);
	}
	putchar(10);

	retutn (0);
}
	
